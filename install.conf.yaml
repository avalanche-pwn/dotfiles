- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: .tmux.conf
    ~/.vim: .vim
    ~/.gitconfig: .gitconfig
    ~/.config/fish: fish

- create:
    - ~/.vim/undodir
    - ~/.vim/sessions
    - ~/.vim/swapfiles

